name: Lint
on: [push, pull_request]
jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Check Markdown
      run: |
        echo "✅ Markdown lint skipped for portfolio"
        exit 0
